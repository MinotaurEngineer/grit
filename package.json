{
  "name": "grit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A lookup tool for army entry requirements built with Preact and Vite.",
  "homepage": "https://MinotaurEngineer.github.io/grit",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "biome lint ./src --apply",
    "format": "biome format ./src --write",
    "check": "biome check ./src --apply",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@preact/signals": "^2.0.1",
    "preact": "^10.26.4",
    "wouter-preact": "^3.6.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@preact/preset-vite": "^2.10.1",
    "gh-pages": "^6.3.0",
    "vite": "^6.2.3"
  }
}
